import { CharacterCounterDirective } from './character-counter.directive';

describe('CharacterCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterCounterDirective();
    expect(directive).toBeTruthy();
  });
});
